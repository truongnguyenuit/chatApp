{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "yarn server",
    "healthcheckPath": "/server/index.js",
    "healthcheckTimeout": 100,
    "restartPolicyType": "never"
  }
}